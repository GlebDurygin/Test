CREATE TABLE IF NOT EXISTS T_GROUP
(
  ID      BIGINT GENERATED BY DEFAULT AS IDENTITY
    (START WITH 1 INCREMENT BY 1) NOT NULL PRIMARY KEY,
  FACULTY VARCHAR(30)             NOT NULL,
  NUMBER  INT                     NOT NULL
);

CREATE TABLE IF NOT EXISTS T_STUDENT
(
  ID          BIGINT GENERATED BY DEFAULT AS IDENTITY
    (START WITH 1001 INCREMENT BY 1) NOT NULL PRIMARY KEY,
  LAST_NAME   VARCHAR(30)            NOT NULL,
  FIRST_NAME  VARCHAR(30)            NOT NULL,
  MIDDLE_NAME VARCHAR(30),
  BIRTHDATE   DATE                   NOT NULL,
  GROUP_ID    BIGINT                 NOT NULL,
  CONSTRAINT FK_STUDENT_GROUP FOREIGN KEY (GROUP_ID) REFERENCES T_GROUP(ID) ON DELETE RESTRICT
);

INSERT INTO T_GROUP (FACULTY, NUMBER)
VALUES ('Информатики', 6401);
INSERT INTO T_GROUP (FACULTY, NUMBER)
VALUES ('Информатики', 6210);
INSERT INTO T_GROUP (FACULTY, NUMBER)
VALUES ('Летательных аппаратов', 3107);
INSERT INTO T_GROUP (FACULTY, NUMBER)
VALUES ('Иностранных языков', 35);
INSERT INTO T_GROUP (FACULTY, NUMBER)
VALUES ('Летальных аппаратов', 3412);

INSERT INTO T_STUDENT (LAST_NAME, FIRST_NAME, MIDDLE_NAME, BIRTHDATE, GROUP_ID)
VALUES ('Петров', 'Александр', 'Александрович', '1997-12-16', 1);
INSERT INTO T_STUDENT (LAST_NAME, FIRST_NAME, MIDDLE_NAME, BIRTHDATE, GROUP_ID)
VALUES ('Волжанин', 'Александр', 'Романович', '1999-07-20', 2);
INSERT INTO T_STUDENT (LAST_NAME, FIRST_NAME, MIDDLE_NAME, BIRTHDATE, GROUP_ID)
VALUES ('Сонин', 'Никита', '', '2000-01-27', 2);
INSERT INTO T_STUDENT (LAST_NAME, FIRST_NAME, MIDDLE_NAME, BIRTHDATE, GROUP_ID)
VALUES ('Олейник', 'Юрий', 'Дмитриевич', '1998-11-16', 5);
INSERT INTO T_STUDENT (LAST_NAME, FIRST_NAME, MIDDLE_NAME, BIRTHDATE, GROUP_ID)
VALUES ('Петрова', 'Александрв', 'Ивановна', '2000-02-10', 3);